# En développement local, laissez tel quel : Vite proxifie /api vers
# votre backend FastAPI local (voir vite.config.js).
# En production, remplacez par l'URL réelle de votre API déployée,
# par exemple : VITE_API_BASE_URL=https://api.nexus-platform.com
VITE_API_BASE_URL=/api
